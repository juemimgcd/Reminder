<script setup lang="ts">import type { MemoryCandidate } from "../../types"; defineProps<{items:MemoryCandidate[];pending:boolean}>(); defineEmits<{action:[item:MemoryCandidate,action:"confirm"|"reject"]}>();</script>
<template><section><h2>Pending review <span>{{ items.length }}</span></h2><article v-for="item in items" :key="item.candidate_id"><p><small>{{ item.memory_type }} · {{ Math.round(item.confidence*100) }}%</small>{{ item.subject }} {{ item.predicate }}: <strong>{{ item.value }}</strong></p><div><button :disabled="pending" @click="$emit('action',item,'confirm')">Confirm</button><button :disabled="pending" @click="$emit('action',item,'reject')">Reject</button></div></article></section></template>
<style scoped>section{display:grid;gap:.5rem}h2{font-size:.85rem}h2 span{color:var(--accent)}article{display:flex;justify-content:space-between;gap:1rem;padding:.7rem;background:var(--bg-panel);border:1px solid var(--border-muted);border-radius:.4rem}p{margin:0;font-size:.75rem}small{display:block;color:var(--text-tertiary)}button{margin-left:.3rem;padding:.3rem .5rem}button:disabled{opacity:.5}</style>
